{
  "dependencies": {
    "react-bootstrap": "^2.7.4"
  },
  "engines": {
    "node": "16.13.1",
    "npm": "9.6.4"
  },
  "name": "express-react-project",
  "version": "1.0.0",
  "devDependencies": {
    "concurrently": "^8.0.1",
    "react-scripts": "^5.0.1",
    "wait-on": "^7.0.1"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "cd server && npm run start",
    "heroku-postbuild": "NPM_CONFIG_PRODUCTION=false client && npm install --prefix client && npm run build --prefix client "
  },
  "author": "",
  "license": "ISC",
  "description": ""
}
